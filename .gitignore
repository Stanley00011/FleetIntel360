# Python
__pycache__/
*.pyc
.venv/
env/

# Data & Database 
# (We will ignore raw data, but keep the analytics DB for Streamlit Cloud)
warehouse/raw/
warehouse/staging/
*.log

# Docker
.dockerignore

# Secrets 
.env
auth.json
*.json
!warehouse/analytics/analytics.duckdb # This '!' tells Git to TRACK this specific filedbt_fleet_intel/target/
infra/terraform/.terraform/
infra/terraform/*.tfstate*
warehouse/raw/
